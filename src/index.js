import React from 'react';
import ReactDOM from 'react-dom/client';
import GoCurrency from './component/GoCurrency';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GoCurrency />
  </React.StrictMode>
);


